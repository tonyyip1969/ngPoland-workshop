/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { UpupPipe } from './upup.pipe';

describe('Pipe: Upup', () => {
  it('create an instance', () => {
    let pipe = new UpupPipe();
    expect(pipe).toBeTruthy();
  });
});
